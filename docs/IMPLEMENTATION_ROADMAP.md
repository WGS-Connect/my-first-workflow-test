# Production implementation

This build implements the new single-channel audiobook architecture directly. The previous finance pipeline and the previous 50-image audiobook visual generator are removed rather than left as alternate production paths.
